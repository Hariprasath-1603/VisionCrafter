# YAML config overrides for training
EPOCHS: 200
BATCH_SIZE: 128
LEARNING_RATE: 0.0002
BETA1: 0.5
BETA2: 0.999
NOISE_DIM: 100
IMAGE_SIZE: 64
IMAGE_CHANNELS: 3
DATASET_ROOT: ./data
NUM_WORKERS: 4
OUTPUT_DIR: ./outputs
SAVE_IMAGE_EVERY: 10
